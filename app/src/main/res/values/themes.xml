<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Allows us to override platform level specific attributes in their
         respective values-vXX folder. -->
    <style name="Platform.Theme.Material3Sample" parent="Theme.Material3.DayNight">
        <item name="android:statusBarColor">@color/black30</item>
    </style>

    <!-- The actual theme we use. This varies for light theme (here),
         and values-night for dark theme. -->
    <style name="Theme.Material3Sample" parent="Platform.Theme.Material3Sample">
        <item name="colorPrimary">@color/blue500</item>
        <item name="colorPrimaryDark">@color/blue800</item>
        <item name="colorAccent">@color/yellow700</item>
    </style>
    <style name="Theme.Material3Sample.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Material3Sample.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.Material3Sample.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>